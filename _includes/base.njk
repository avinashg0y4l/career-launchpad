<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title> {# Uses the title from the markdown file's front matter #}
    {# REMOVED THE INCORRECT LINE #}
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}"> {# KEEP THIS CORRECT LINE #}
  </head>
  <body>
    <header>
      <h1><a href="{{ '/' | url }}">Student Opportunity Hub</a></h1> {# ADDED | url filter #}
      {# Add navigation links here later if needed #}
    </header>

    <main>
      {{ content | safe }} {# This is where the markdown content will be inserted #}
    </main>

    <footer>
      <p>© {% year %} Your Name/Site Name</p> {# Make sure you have the year shortcode in .eleventy.js #}
    </footer>
  </body>
</html>